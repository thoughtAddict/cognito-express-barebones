<% layout('layout') -%>

<% if ( user.signedIn ) { %>
You are Logged in: <%= user.username %> (<a href="/profile">Your Account Profile</a>)
<br/>
<% } else { %>
You are not Logged in.
<br/>
<% } %>

<br/>
<% if ( !user.signedIn ) { %>
<a href="/signup">Register</a>
<% } %>
<br/>
<br/>
<a href="/securePage">Secure Page</a>
<br/>
<br/>
<% if ( user.signedIn ) { %>
<a href="/logout">Sign Out</a>
<% } else { %>
<a href="/login">Sign In</a>
<% } %>

